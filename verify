#!/bin/sh
set -eu
root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
(cd "$root/model" && sha256sum -c MANIFEST.sha256)
(cd "$root/results/reference" && sha256sum -c SHA256SUMS)
echo "Diagnostic model inputs and reference results: verified."
